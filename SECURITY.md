# SECURITY.md — 키 노출 비상 대응 매뉴얼

> 이 문서는 `CLAUDE.md`(현재 파일명: `clade-md`)의 "🚨 비상시 — 키 노출 의심" 규칙에서
> 안내하는 매뉴얼입니다. API 키, 비밀번호, SSH 키가 외부에 노출됐을 수도 있다는
> 의심이 들면 이 순서대로 처리하세요. 초보자도 따라 할 수 있도록 쉬운 말로 적었습니다.

---

## 🚨 키 노출이 의심될 때 — 지금 바로 할 일 (순서대로)

침착하게 아래 순서를 따르세요. **속도가 가장 중요합니다** — 정리는 나중에 해도 되지만, 폐기는 지금 해야 합니다.

### 1단계. 어떤 키가 노출됐는지 확인
아래 중 어디에 해당하는지 먼저 파악합니다.
- **OpenRouter** API 키 (`sk-or-v1-...`로 시작)
- **Oracle 서버** SSH 키 (`~/.ssh/oracle-server.key`)
- **WordPress** 관리자 비밀번호 / 애플리케이션 비밀번호
- 그 외 (`.env` 안의 다른 키)

어디서 노출됐는지도 함께 확인하세요: 코드에 하드코딩되어 GitHub에 올라감 / 채팅·이미지로 공유됨 / 화면 공유 중 노출됨 등.

### 2단계. 즉시 폐기 (Revoke)
노출된 키는 **바로 무효화**합니다. 새 키를 만들기 전에, 옛 키부터 끄는 게 우선입니다.
- **OpenRouter**: 계정 설정의 API Keys 메뉴에서 해당 키를 삭제(Revoke)합니다.
- **Oracle 서버 SSH 키**: OCI 콘솔에서 해당 인스턴스에 등록된 공개키를 제거하거나, 인스턴스의 authorized_keys에서 해당 키를 삭제합니다.
- **WordPress**: 관리자 프로필 → 애플리케이션 비밀번호(Application Passwords)에서 해당 비밀번호를 삭제하고, 필요하면 로그인 비밀번호도 즉시 변경합니다.

### 3단계. 재발급
폐기가 끝난 후에만 새 키를 발급받습니다.
- **OpenRouter**: 새 API 키 생성.
- **Oracle**: 새 SSH 키 쌍(`oracle-server-new.key` 등)을 생성해 인스턴스에 새로 등록.
- **WordPress**: 새 애플리케이션 비밀번호 생성 또는 관리자 비밀번호 재설정.

### 4단계. 환경변수 교체
`~/claude-workspace/.env` 파일을 열어 옛 키를 새 키로 교체합니다.
- `.env` 파일은 **절대 공유하지 마세요** (Claude Code에게도 내용 그대로 보여달라고 요청하지 마세요 — 마스킹된 값만 확인).
- 코드 안에 키가 직접 적혀 있지 않은지(하드코딩) 함께 확인합니다.

### 5단계. 사용 이력 확인 (악용 여부 점검)
키가 실제로 남에게 사용됐는지 확인합니다.
- **OpenRouter**: 대시보드의 사용량(Usage) 페이지에서 본인이 쓴 적 없는 요청·비용이 있는지 확인.
- **Oracle**: 콘솔의 감사 로그(Audit Log) 또는 서버 접속 로그(`/var/log/auth.log` 등)에서 낯선 IP의 접속 기록 확인.
- **WordPress**: 관리자 화면의 로그인 이력 또는 보안 플러그인 로그에서 낯선 로그인·게시물 변경 여부 확인.

### 6단계. 이미 Git에 올라갔다면
`git add`/`git commit`/`git push`로 이미 키가 저장소에 들어갔다면:
- 1~5단계(폐기 → 재발급 → 교체 → 이력 확인)를 **먼저** 끝냅니다. 키를 무효화하면 히스토리에 남아 있어도 위험도가 크게 줄어듭니다.
- 그 다음 Claude Code에게 "git 히스토리에서 키 제거해줘"라고 요청하세요. 히스토리를 고치는 작업(`filter-repo`, `force push` 등)은 되돌리기 어려운 작업이라 반드시 먼저 물어보고 진행합니다.

---

## 🛡️ 평소 예방 체크리스트

- [ ] `.gitignore`에 `.env`, `*.key`, `*.pem`, `id_rsa`, `credentials` 패턴이 들어있는지 확인
- [ ] `git add` 하기 전 `git status`로 어떤 파일이 올라가는지 확인
- [ ] 코드에는 키를 직접 쓰지 않고 항상 환경변수(`.env`)로만 참조
- [ ] 키를 화면 공유·캡처·채팅으로 보여줄 때는 마스킹(`sk-or-v1-***`) 확인
- [ ] 외부 서비스(챗봇, 온라인 도구 등)에 파일이나 로그를 올리기 전, 민감정보 포함 여부 확인

---

## 📌 연락처 / 계정 메모 (직접 채워 넣기)

| 서비스 | 계정 확인 방법 | 비고 |
|---|---|---|
| OpenRouter | [ ] | |
| Oracle Cloud | [ ] | 서버 별칭: `oracle-server` |
| WordPress | [ ] | |

> 이 표는 비상시 빠르게 참고하기 위한 개인 메모용입니다. 실제 키 값은 절대 적지 마세요.
